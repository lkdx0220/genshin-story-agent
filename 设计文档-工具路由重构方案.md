# 工具路由重构方案

## 目标

24 个工具 → 按意图分组动态注入，每次 Agent 运行只暴露 2-10 个工具。

## 核心思路

不合并工具实现，不加新的索引层。唯一新增的是一层**极轻量路由器**（一次 LLM 调用，输出几个字母），在 Plan Agent 启动前决定本次对话暴露哪些工具。

```
当前:  用户 → Plan Agent(24个工具) → Answer
       ↑ LLM 每次从 24 个中选，选错概率高，Plan Prompt 需大量篇幅做意图分类

改后:  用户 → 路由器(1次轻量LLM) → Plan Agent(2-10个工具) → Answer
       ↑ 路由输出 A~F 或 ALL 标签 → 只注入对应组工具 → Plan Prompt 大幅精简
```

## 工具分组

### 组 A：搜索检索（2 个）
`hybrid_search`, `search_activity`
- "找一下"、"搜"、"有没有提到"、"哪里出现过"
- 也作为兜底常驻注入到所有组（见下方"跨组处理"）

### 组 B：角色查询（6 个）
`query_character`, `list_characters_by_element`, `list_characters_by_region`, `list_characters_by_weapon`, `list_characters_by_rarity`, `count_character_lines`
- 角色信息、按属性/地区/武器筛选、台词统计
- **特别注意**：问题主语是角色/NPC + 询问其过去经历或演变过程（而非"当前状态/属性"）时，必须加 D。见路由器规则 2。

### 组 C1：装备养成（5 个）
`query_weapon`, `query_artifact`, `query_material`, `query_recipe`, `query_food`
- 武器、圣遗物、材料、食谱、食物

### 组 C2：世界生态（5 个）
`query_monster`, `query_collectible`, `query_region`, `query_concept`, `list_collectibles_by_region`
- 怪物、采集物、地区、概念/组织

### 组 D：剧情任务（3 个）
`query_story`, `query_quest`, `load_quest_content`
- 主线、传说任务、世界任务、活动剧情、任务内容

### 组 E：书籍文献（2 个）
`load_book_content`, `get_book_metadata`
- 游戏内书籍、书的内容、作者、版本

### 组 F：溯源追踪（1 个）
`find_first_mention`
- "第一次出现"、"首次登场"、"最早提到"

### 组 ALL：全量工具（特殊标签）
- 用户问题含"所有/全部/汇总/一手资料/各方面/全貌"等全局词汇时触发
- 直接注入全部 24 个工具，不限制

### 总计：24 → 分组后每次 2-10 个（双标签）+ 1 个常驻 hybrid_search

---

## 路由器设计

### 模型选择

用 `qwen-plus` 或 `qwen-turbo`（非 max），纯分类任务不需要强模型。

### 路由 Prompt（完整版，含攻击修复）

```
你是一个意图分类器。根据用户问题，输出 1-2 个意图标签，或特殊标签 "ALL"。

===== 标签列表 =====

A  —— 搜索检索：问题要求"搜索/查找/有没有提到/在哪出现"某段文本，未指定已知的实体类型
B  —— 角色查询：问角色的信息、属性、按元素/地区/武器筛选、角色台词统计
C1 —— 装备养成：问武器、圣遗物、材料、食谱、食物的详情
C2 —— 世界生态：问怪物、采集物、地区介绍、游戏概念/组织的详情
D  —— 剧情任务：问主线剧情、传说任务、世界任务、活动剧情、任务讲了什么、角色在任务中做了什么
E  —— 书籍文献：问游戏内书籍的内容、作者、版本
F  —— 溯源追踪："第一次出现/首次登场/最早提到/在哪出场"
ALL—— 全量工具：问题明确要求跨多个维度的综合检索

===== 判别规则 =====

1. "XX讲了什么" → 判断 XX 是书名还是任务名：
   - 看起来像书名（如"日月前事"、"白夜国馆藏"、"浮槃歌卷"）→ E
   - 看起来像任务名（如"捕风"、"不动鸣神"、"向沙中去"）→ D
   - 无法判断 → ["D", "E"]

2. 问题主语是角色/NPC + 询问其过去经历或演变过程 → ["B", "D"]
   触发词包括但不限于："经历/成长/变化/做了什么/在哪出现/故事/怎么来的/
   身世/来历/为什么变成这样/和XX的关系/如何成为"
   简言之：问的是"一个角色/NPC 的过去经历或演变过程"，而非"当前状态/属性"→ 加 D。
   示例："蒂蕾娜经历了什么" → ["B", "D"]
   示例："钟离在璃月主线做了什么" → ["B", "D"]
   示例："阿佩普的绿洲守望者是怎么来的" → ["C2", "D"]（主语是怪物/NPC，
         "怎么来的"是询问来历/过程，query_monster 词条不足以覆盖，需任务全文）

3. 问题中有明确任务名 → 必须包含 D
   示例："《巨龙与自由的变奏》讲了什么" → ["D"]
   示例："《白之公主与六侏儒》讲了什么" → ["D", "E"]
         （该名称看起来像书但实际上是世界任务中的叙事文本，无法确定 → 双标签）
   示例："浮槃歌卷讲了什么" → ["D", "E"]

4. "有哪些/列出/筛选" + 明确的筛选维度（元素/地区/武器等）→ B
   示例："有哪些火元素角色" → ["B"]
   示例："蒙德有哪些特产" → ["C2"]

5. 问题含"原话/原文/台词/说过" → 判断主语：
   - 主语是角色名 + 在某个任务中 → ["B", "D"]
   - 主语是角色名，未指定任务 → ["B", "A"]

6. 引述验证（"这段话在库里吗"、"原文检索'...'"）→ ["A"]

7. 对比分析（"A和B有什么异同/变化"）→ 判断A、B的实体类型：
   - 两个都是任务/剧情 → ["D"]
   - 一方是任务一方是角色 → ["B", "D"]
   - 对比角色在不同任务中的表现/态度变化 → ["B", "D"]
   - 两本不同的书 → ["E"]

8. 主观评价/价值判断（"怎么看"、"做得对不对"）→ 按主题正常分类，
   不要因为"降级研究"改变路由

9. 问题含"所有/全部/汇总/一手资料/各方面/全貌/完整呈现"等全局词汇 → ["ALL"]
   示例："帮我汇总'命运的织机'的所有一手资料" → ["ALL"]
   示例："关于坎瑞亚的全部信息" → ["ALL"]
   这直接触发全量工具模式，不限制工具集。

===== 兜底规则 =====

- 拿不准时，多输出一个标签比少输出好
- 如果只能输出一个标签但犹豫，输出最可能的那一个
- 不要输出超过 2 个标签（除非输出 ["ALL"]）

===== 多轮对话规则 =====

如果提供了对话历史，结合最近一轮对话判断用户当前问题的指代对象。
特别注意：对话历史摘要中的实体链（如"小女孩=幽灵小九九=胡桃传说任务"），
在当前问题指代模糊时必须追溯。

===== 输出格式 =====

只输出 JSON 数组，不要其他内容。示例：
["B", "D"]
["A"]
["E"]
["ALL"]

当前是第几轮对话：{turn_number}
对话历史摘要：{conversation_summary}
用户问题：{user_question}
```

### 路由调用示例（Few-shot，嵌入 Prompt 或分开管理）

| 用户问题 | 路由输出 | 说明 |
|----------|----------|------|
| 护摩之杖是什么 | ["C1"] | |
| 原神有哪些火元素角色 | ["B"] | |
| 蒙德有哪些特产 | ["C2"] | |
| 捕风讲了什么 | ["D"] | |
| 日月前事全文 | ["E"] | |
| 钟离第一次出场是在哪 | ["F"] | |
| 搜一下"药君 魇神" | ["A"] | |
| 浮槃歌卷讲了什么 | ["D", "E"] | 规则1：无法确定是书还是任务 |
| 蒂蕾娜经历了什么 | ["B", "D"] | 规则2：行为词触发 |
| 阿佩普的绿洲守望者是怎么来的 | ["C2", "D"] | 规则2：主语是怪物/NPC，"怎么来的"是隐性行为词 |
| 胡桃在传说任务里说了几句话 | ["B", "D"] | |
| 胡桃的传说任务是哪个 | ["D"] | |
| 《巨龙与自由的变奏》讲了什么 | ["D"] | |
| 《白之公主与六侏儒》讲了什么 | ["D", "E"] | 规则1：像书名但实际是任务叙事文本 |
| 艾莉丝和钟离对待命运的态度有什么异同 | ["D"] | |
| 对比《我们终将重逢》和《卡利贝尔》里戴因斯雷布的态度变化 | ["B", "D"] | 规则7：角色+多任务对比 |
| 千夜浮梦的背景故事是什么 | ["C1"] | |
| 渊下宫的故事 | ["C2", "D"] | |
| 雷电影的治国方式做得对不对 | ["D"] | 规则8：不因"降级研究"改变路由 |
| 给我贴出《日月前事》的原文 | ["E"] | |
| 帮我数数捕风里温迪说了几句话 | ["B", "D"] | |
| 这段话在库里吗："魔神战争开始前夕，药君与魇神交谈" | ["A"] | |
| 天理的维系者最早在哪提到 | ["F"] | |
| 急冻树抗性是什么 | ["C2"] | |
| 绝缘之旗印效果 | ["C1"] | |
| 有哪些蒙德角色 | ["B"] | |
| 胡桃用什么武器 | ["B"] | |
| 林间风这本书的作者是谁 | ["E"] | |
| 若陀龙王是什么 | ["C2"] | |
| 深渊教团为什么要毁灭七国 | ["C2"] | query_concept 不够时 Plan Prompt 会补 hybrid_search |
| 帮我找一下"命运的织机"所有的相关资料 | ["ALL"] | 规则9：全局词汇 |
| 关于坎瑞亚的全部信息 | ["ALL"] | 规则9：全局词汇 |

---

## 跨组处理

### hybrid_search 常驻注入

A 组的 `hybrid_search` 作为全局兜底，**始终注入到所有组的工具列表**。原因：
- C2 的概念查询（如"深渊教团动机"）最佳答案在编年史/任务文本中，不在 query_concept 词条里
- 用户问题可能跨实体类型，Plan Agent 需要一个"万能搜索"出口
- 只多 1 个工具，不影响选择负担

### 多标签合并

路由器可输出最多 2 个标签，或 `["ALL"]`。合并逻辑：取并集，去重。

```python
def get_tools_for_intent(labels):
    if "ALL" in labels:
        return get_all_tools()
    tools = set()
    for label in labels:
        tools.update(TOOL_GROUPS[label])
    tools.add(hybrid_search)  # 常驻兜底
    return list(tools)
```

示例：
- `["B", "D"]` → B 组(6) + D 组(3) + hybrid_search(1) = **10 个工具**
- `["D", "E"]` → D 组(3) + E 组(2) + hybrid_search(1) = **6 个工具**
- `["A"]` → A 组(2) = **2 个工具**（hybrid_search 已在 A 组中）
- `["ALL"]` → 全部 24 个工具

### 兜底：工具不够用时扩大

两种触发机制：

**触发 1：连续失败**（已有）
```python
if consecutive_failures >= 2:
    return "INSUFFICIENT_TOOLS"
```

**触发 2：用户显式要求全局检索**（新增，规则 9）
```python
# 路由器识别全局词汇 → 直接输出 ["ALL"]
# 不需要等 Plan Agent 连续失败再触发
```

当触发 `INSUFFICIENT_TOOLS` 时：
```python
tools = get_all_tools()  # 回退到全部 24 个
retry()
```

---

## Plan Prompt 补丁（必须添加的规则）

以下规则必须在 Plan Prompt 中明确写出，否则即使路由正确，Agent 也会在工具使用层面犯错。

### P1：专属工具失败后的降级补位

```
当任何专属查询工具（query_xxx、load_book_content）返回"未找到"或空结果时，
必须立即用 hybrid_search 搜索同一关键词再试一次，不得直接判定为"未收录"。
```

对应攻击：攻击1（书名伪装）

### P2：简介深度不足时的补位

```
query_concept 和 query_monster 返回的是简短词条/图鉴（通常 < 200 字），
不包含深层动机、历史沿革、具体事件。
如果用户问的是"为什么/原因/动机/历史/过程/怎么来的"，
在调完 query_xxx 后必须再用 hybrid_search 搜索同一主题，
获取更详细的编年史/任务文本。
```

对应攻击：攻击2（隐性行为词）、攻击5（伪百科）

### P3：对比分析强制全文加载

```
当问题涉及"对比/变化/异同"时，必须先加载所有涉及文本的全文（load_quest_content 或 load_book_content），
严禁仅用 hybrid_search 片段或 query_quest 摘要进行对比。
```

对应攻击：攻击6（组合拳）

---

## 会话摘要生成约束（必须修改）

### 当前问题

多轮对话中，摘要如果只是"用户在问胡桃传说任务"而没有保留"小女孩=幽灵小九九=胡桃传说任务里的角色"这条实体链，第 3 轮之后路由器必然丢失指代。

### 修复方案

在 `_save_session_to_disk` 的摘要生成 prompt 中加约束：

```
对话摘要必须保留每轮提到的具体实体名称及其指代关系。
例如："用户问胡桃传说任务中的幽灵小九九，接着问了这个小女孩的歌"
而非："用户问了胡桃相关的内容，然后追问了角色歌曲"

保留规则：
- 每个新引入的实体（角色名、任务名、书名、物品名）必须在摘要中出现
- 跨轮指代（"她"→"小九九"、"那个"→"层岩巨渊"）必须写出解析后的名称
- 如果摘要因为长度限制需要剪裁，优先保留最新 2 轮的完整实体信息
```

对应攻击：攻击4（多轮指代漂移）

### 实现位置

`genshin_story_web_api.py` 的 `_save_session_to_disk` 函数中，`_llm_summarize` 调用处。

---

## 边界情况与防御策略（完整版）

| # | 攻击 | 场景示例 | 风险 | 防御层 | 改动类型 |
|---|------|----------|------|--------|----------|
| 1 | 书名伪装 | 《白之公主与六侏儒》讲了什么 | 路由器输出 ["E"]，load_book_content 失败后放弃 | P1（Plan 补位规则）| Prompt |
| 2 | 隐性行为词 | 阿佩普是怎么来的 | "怎么来的"不在枚举列表，只路由到 C2 | 规则2 改为语义描述 + P2（简介补位）| 路由 Prompt |
| 3 | 跨层追问 | 裁叶萃光背景里的学者是谁 | C1 组工具返回碎片，hybrid_search 需精准召回 | 工具返回中标注关联角色名（现有数据已含）| 工具返回质量 |
| 4 | 多轮指代 | 第3轮："她唱的歌原文是什么" | 摘要丢失实体链，路由器无法解析指代 | 会话摘要生成约束 | 代码层 |
| 5 | 伪百科 | 深渊教团为什么要毁灭七国 | query_concept 太短，Agent 认为就是答案 | P2（简介补位）| Prompt |
| 6 | 组合拳 | 对比戴因在两个任务中的态度变化 | 只路由到 D，漏 B；可能不加载全文 | 规则7 补"角色+多任务" + P3（强制全文加载）| 路由 + Plan Prompt |
| 7 | 跨组全量 | 命运织机的所有一手资料 | 最多 2 个标签，无法覆盖所有组 | 规则9 ["ALL"] | 路由 Prompt |

---

## 实现步骤

1. **新建 `intent_router.py`**
   - 路由器类 + `ROUTER_PROMPT`（含全部 9 条规则 + few-shot 示例）
   - `TOOL_GROUPS` 映射
   - `get_tools_for_intent(labels)` 函数

2. **修改 `llm_with_tools` 生成逻辑**
   - 从固定 `llm.bind_tools(tools)` 改为按 intent 动态 bind
   - 在 `plan_node` 调用前完成路由判断

3. **修改 `plan_node`**
   - 进入前先调路由器，拿到 labels 后组装 tools
   - 保留 `INSUFFICIENT_TOOLS` 信号处理
   - 新增 `["ALL"]` → 直接全量工具的分支

4. **精简 Plan Prompt**
   - 移除意图分类逻辑（当前第 38-91 行）
   - 添加 P1、P2、P3 补丁规则
   - 保留工具选择策略和熔断规则

5. **修改会话摘要生成逻辑**
   - `_save_session_to_disk` 的摘要 prompt 加入实体保留约束
   - 验证：3 轮以上指代不丢失

6. **工具返回质量提升**（可选，不急）
   - `query_weapon` 返回中附加"更多背景见任务【XX】"
   - `query_monster` 返回中附加"详细剧情请见相关任务"

---

## 预期收益

| 指标 | 当前 | 改后 |
|------|------|------|
| LLM 工具选择空间 | 24 个 | 2-10 个（["ALL"] 时 24 个） |
| Plan Prompt 长度 | ~270 行 | ~190 行（移除意图分类，加 3 条补丁） |
| 工具选错概率 | 较高（24 选 1） | 较低（10 选 1） |
| 额外 LLM 调用 | 0 | +1（极轻量，qwen-turbo，~100 token） |
| 多轮指代丢失率 | 3 轮后大概率 | 显著降低（实体保留摘要） |
| 兜底覆盖 | 仅连续失败 | 连续失败 + 用户显式 ["ALL"] |
